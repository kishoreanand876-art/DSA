class Solution {
    public int shortestSubarray(int[] nums, int k) {
        int count=0;
        int sum=0;
    for(int i=0;i<nums.length;i++){
       sum+=nums[i];
       if(sum>=k){
        return count;
       }else{
       count++;
        return -1;
       }
